feudal_government_taxes = {
	display_mode = tree
	icon = gold_icon
	obligation_levels = {
		feudal_tax_exempt = {
			position = { 0 0 }

			tax = exempt_feudal_tax
			vassal_opinion = 10

			ai_liege_desire = 1
			ai_vassal_desire = 5
			score = 2
		}
		feudal_tax_low = {
			parent = feudal_tax_exempt
			position = { 1 0 }

			tax = low_feudal_tax
			vassal_opinion = 5

			ai_liege_desire = 2
			ai_vassal_desire = 4
			score = 1
		}
		feudal_tax_normal = {
			default = yes
			parent = feudal_tax_low
			position = { 2 0 }

			tax = normal_feudal_tax

			ai_liege_desire = 3
			ai_vassal_desire = 3
			score = 0
		}
		feudal_tax_high = {
			parent = feudal_tax_normal
			position = { 3 0 }
			
			tax = high_feudal_tax
			vassal_opinion = -40

			ai_liege_desire = 4
			ai_vassal_desire = 2
			score = -1
		}
		feudal_tax_extortionate = {
			parent = feudal_tax_high
			position = { 4 0 }
			
			tax = extortionate_feudal_tax
			vassal_opinion = -60

			ai_liege_desire = 5
			ai_vassal_desire = 1
			score = -2
		}
	}
}

feudal_government_levies = {
	display_mode = tree
	icon = soldier_icon
	obligation_levels = {
		feudal_levies_exempt = {
			position = { 0 0 }

			levies = {
				value = 0
				if = {
					#if = {
					#	limit = {
					#	scope:vassal = { 
					#	
					#	liege = { highest_held_title_tier >= tier_kingdom }
					#	#highest_held_title_tier < tier_duchy
					#	NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = scope:liege }
					#
					#	}
					#		}
					#	add = -1000
					#}
					if = {
						limit = {
						scope:vassal = { 
						is_at_war = yes

						}
				}
						add = -1000
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { is_at_war = no }
						NOT = { highest_held_title_tier < tier_county }

						}
				}
						add = -1000
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 20 }
						ai_boldness < 30
						
						}
						
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 40 }
						ai_boldness < 30
						scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
						liege = { highest_held_title_tier < tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 60 }
						ai_boldness < 30
						scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
						liege = { highest_held_title_tier < tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 80 }
						ai_boldness < 30
						scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
						liege = { highest_held_title_tier < tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread >= 95 }
						ai_boldness < 30
						scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
						liege = { highest_held_title_tier < tier_kingdom }

						}
				}
						add = 0.02
					}
				if = {
						limit = {
						scope:vassal = { 
						ai_honor >= 25
						OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
				if = {
						limit = {
						scope:vassal = { 
						ai_honor >= 50
						OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 15
							}
						OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 30
							}
						OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 50
							}
							OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 70
							}
							OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 90
							}
							OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 100
							}
							OR = {
							liege = { highest_held_title_tier < tier_kingdom }
							scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege 
							}

						}
				}
						add = 0.1
					}
					if = {
						limit = {
						scope:vassal = { 
						highest_held_title_tier < tier_county

						}
				}
						add = 1
					}
					
					
					####LP/King
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 20 }
						ai_boldness < 30
						
						}
						
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 40 }
						ai_boldness < 30
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 60 }
						ai_boldness < 30
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread > 80 }
						ai_boldness < 30
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						liege = { dread >= 95 }
						ai_boldness < 30
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
				if = {
						limit = {
						scope:vassal = { 
						ai_honor >= 25
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
				if = {
						limit = {
						scope:vassal = { 
						ai_honor >= 50
						NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 15
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 30
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 50
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 70
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 90
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					if = {
						limit = {
						scope:vassal = { 
						opinion = {
								target = liege
								value >= 100
							}
							NOT = { scope:vassal.primary_title.title_capital_county.de_jure_liege.holder = liege }
						liege = { highest_held_title_tier >= tier_kingdom }

						}
				}
						add = 0.02
					}
					
					
				}
				
			}

			ai_liege_desire = 0
			ai_vassal_desire = 0
			score = 0
		}
		
	}
}
